name: scheduling_backend
description: Consultation scheduling backend for the mental-health app (Nyna Connect).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

# The runtime only needs the Dart SDK. The network integration uses dart:io.
# Dev/test dependencies are used by `dart test`.
dependencies:

dev_dependencies:
  test: ^1.29.0
  http: ^1.6.0
  mocktail: ^1.0.5